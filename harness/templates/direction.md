# <Direction title>

**Tag:** `[in-frame]` | `[reframe]`
**Status:** proposed | active | in progress | done | dropped
**Serves:** <which research sub-question>
**Proposed:** YYYY-MM-DD

## The question

One paragraph. What do we not know, and what would this experiment settle? State the
hypothesis and — equally — what the result would look like if the hypothesis is wrong.

## Why it is worth doing now

What it unblocks, what it controls for, or what current claim it would strengthen or break.

## Bootstrap (cold-start runnable)

**This brief must run from a fresh session with no live state from any other notebook.**
Name exactly what to load or compute, with paths:

- checkpoint(s):
- dataset / split:
- derived objects to fit or compute:
- helper functions and where they live:
- setup steps:

## Method

Numbered steps, concrete enough to execute without further design decisions.

## Measurement

- **Metrics** (copy exact name, formula, units, better-direction from the thread registry;
  define any new one here in full):
- **Controls / baselines** (mandatory — what is the no-intervention arm?):
- **Decision rule** — if this brief asks a binary question, state the rule *before* running.
- **Interpreted magnitudes** — state units and the reference scale they are read against.

## Visualization

- The canonical qualitative panel, with these arms as columns:
- Figures, and what each must make visible:
- Both plots **and** printed tables — never tables alone.

## What would falsify the hypothesis

Be explicit. A brief with no failure condition tends to produce a result that confirms
whatever was expected.

## Expected artifacts

- notebook:
- scratch note:
- exported figures:
