# <Thread> — canonical RUN REGISTRY

**The single source of truth for what every run name means.** No deliverable may use a run
code without copying its row into its own definitions table, and figures must use the
**descriptive label**, not the raw code.

Adding a run means adding its row **in the same commit**.

## Naming key

State what every suffix and abbreviation encodes, e.g. trailing digit = seed, `b` = second
seed, `s` = strong configuration. A suffix whose meaning is not on this page is a bug.

## Runs

| code | descriptive label (use this in figures) | objective / task | data | architecture | training | seed | purpose / what it controls for |
|---|---|---|---|---|---|---|---|
| `<code>` | `<self-describing label>` | | | | | | |

## Superseded / do not cite

| code | why | use instead |
|---|---|---|
| | | |
