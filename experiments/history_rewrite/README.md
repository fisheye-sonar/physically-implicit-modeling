# history_rewrite — MOVED 2026-09-19 → `paper/figs/history_rewrite/`

The script (`make_figure.py`, was `scripts/rewrite_history_im.py`) and its outputs now live with the
paper's other figure scripts, so the paper does not depend on `experiments/`.
