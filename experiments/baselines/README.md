# baselines

**Question:** the two decodability floors (observation history; random-init) per instance × arch — Table 3. **Status:** done 2026-09-01, per-arch keyed (b2). Canonical code is `pim/probes/baselines.py`; `scripts/smoke_baselines.py` is the tiny end-to-end check. Results live in `runs/_baselines/<instance>/baselines.json` (consumed by the tables). Finding: `research/findings/decodability-baselines.md`.
