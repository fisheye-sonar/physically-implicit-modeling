# noise_ablation

**Question:** is discworld's editability limited by noise? **Status:** done 2026-08-31 — `dw-noiseless` at 20M, Transformer-L: prediction 21× better, editability unchanged. `drivers/dw_noiseless.sh` is the full unattended chain (generate → train → score). Run: `runs/noise_ablation/L-dw-noiseless-20m`; logs `logs/noise_ablation/`.
