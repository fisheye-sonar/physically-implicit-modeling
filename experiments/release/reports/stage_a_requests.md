# Stage A requests, grouped by owner

## to core
- from **rayworld**, `pim/metrics/zone_editability.py`: The docstring near L67 still names pim/environments/rayworld/render2d.py; remove that reference. sim_config_from passing the omni2d fields is fine (the fields are kept).
- from **othello**, `pim/editors/pinv.py, pim/editors/grad_steer.py, pim/models/*`: Keep pinv_step's default space='zspace' and make_intervention_hook's default optimizer='adam', or remove those parameters. The Othello arms no longer pass them. Keep WorldStateProbe.n_classes, model.decode(idx, edit=hook), model.logits, model.residual_stack and model.n_layers. Keep load_checkpoint legacy rule 4: the standard Othello checkpoint has no 'arch' key.
- from **export**, `pim/probes/base.py`: Keep pim.probes.base.WorldStateProbe importable at that path with its current pickled attribute layout: all 1198 shipped probe pickles reference it (plus torch.nn Linear/Sequential/ReLU). Checkpoints reference only torch and collections.

## to export
- from **rayworld**, `experiments/release/export_artifacts.py`: Strip the out-of-scope categorical blocks from the shipped scores.json: obs5 appearance-fac, appearance-d*/-lat, grid-4x2/8x4/32x16, and 5/16-ray appearance. Freshly written values now read: grid_selection rule without ', 2026-09-12'; bench_selection.file under datasets/rayworld/<inst>/.../selection.json; bayes_floor version '1.0'.
- from **core**, `experiments/release/export_artifacts.py`: Exclude the 112 dead probe-cache files listed in experiments/release/work/core/dead_probe_files.tsv: 18 pickle pim.probes.nullspace.NullspaceCascade (cannot be unpickled in the release), 84 in adjacent-flip __seed1 / standard __seed1 / standard __seed2 are keyed on non-shipped (390k) weights, 10 baseline files are keyed on random-init models of unshipped architectures. Also: PRIVATE runs/initial_othello_comparison/L-oth-20m/scores.json is missing since 22:14 today; ship from your recovered copy.

## to infra
- from **rayworld**, `.gitignore`: Add `.scratch/`: arms._scratch_dir() memory-maps residual stacks at the repo root during probe and inverse-map fits.

## to scoring
- from **rayworld**, `pim/scoring/rayworld.py`: Delete the ND loops that call rwa.nanda_arm (about L123) and tkb.nanda_arm (about L184); both functions are gone.
- from **rayworld**, `pim/environments/layout.py`: Set DEFAULT_INSTANCE['rayworld'] = 'standard'. Keep these callable as used by rayworld: REPO, DEFAULT_INSTANCE, instance_root(cls, inst), train_dir(cls, inst), eval_file(cls, inst), edits_file(cls, inst), edits_selection(cls, inst), probe_file(cls, inst, size), probe_manifest(cls, inst, size). probe_key(cls, inst, size) must keep returning ('rayworld/<inst>', 'probe_<size>'). Rayworld no longer calls legacy_probe_key, legacy_edits_instance, parse_dataset_path, ensure_marker or edits_dir.
- from **othello**, `pim/scoring/othello.py`: In othello_arms, remove the ('add_sub', 'ND', a_nd) entry: oa.linear_arm now raises ValueError for any mode other than 'pinv'. Remove the oth_extra_targets (mine_signed) block loop, since fit_probe_grid and flatten_rows now reject that target. Drop ND from probe_sources.
- from **othello**, `pim/environments/layout.py`: Keep othello_split_dir(inst, name), with OTH_ROLE including 'edits' -> 'edits'. Keep othello_cases_file(inst, n_cases=1000), resolving to datasets/othello/<inst>/edits/cases_<n>.pkl (edits/ flattened, no version level); bench.cases_path calls it with the instance only. corpus.build no longer calls ensure_marker, so it can be removed.
- from **core**, `pim/scoring/baselines.py`: Line 47 comment mentions transformer_s (n_layers*(window-1)+1); rewrite it for Transformer-L only.
- from **export**, `notebooks/master_eval.ipynb (SETTINGS) / pim/scoring/blocks.py`: Set rw_extra_targets = {'rayworld/8-ray': ('appearance-fac','appearance','grid-6x5','grid-10x3','grid-16x8','pos@appearance'), 'rayworld/128-ray': ('appearance-fac',), 'rayworld/16-ray': ('appearance-fac',), 'rayworld/5-ray': ('appearance-fac',), 'rayworld/8-ray-tokens': ('appearance-fac',), 'rayworld/8-ray__seed0': ('appearance-fac',), 'rayworld/8-ray__seed1': ('appearance-fac',), 'rayworld/8-ray__seed2': ('appearance-fac',)}. The explicit replicate entries matter: without them the replicates inherit pos@appearance through replicate.of, and pos@appearance (a regression target) would be refit inline; the replicates ship no pos@appearance block or probes.
- from **export**, `notebooks/master_eval.ipynb (SETTINGS)`: oth_extra_targets = () (Othello scores.json bases is {} after mine_signed was removed; otherwise mine_signed is refit inline). rw_bases_by_instance must map the obs5 instance (shipped instance 'obs5', env 'rayworld') to ('cartesian',).
- from **export**, `notebooks/master_eval.ipynb, pim/scoring/baselines.py, pim/scoring/blocks.py, pim/environments/prediction.py, pim/environments/*/bayes.py`: EVAL_VERSION_BY_ENV = {'rayworld': '1.0', 'othello': '1.0'}, EVAL_VERSION = '1.0', BASELINE_VERSION = '1.0', IM_VERSION = '1.0', PRED_VERSION = '1.0', both FLOOR_VERSION = '1.0' (every shipped artifact stores '1.0'). Write probe_dir repo-relative ('runs/<id>/probes'), and do not write settings / commit_sha / dated blocks_added or inverse_added records, so a re-score stays anonymous.
- from **export**, `notebooks/master_eval.ipynb (SETTINGS)`: Note: 8-ray transformer_l has no grid-6x5/grid-10x3/grid-16x8 baseline floors (never fitted in PRIVATE either). If those targets are required of the baselines, the baselines dry run prints a WOULD-fit (require_cached, nothing new), as PRIVATE did.

## to scripts
- from **rayworld**, `scripts/generate_dataset.py`: Remove --omni2d/--omni2d-h/--omni2d-w, --soft-edge, --soft-psf-sigma, --soft-occlusion-temp, the 'lambert' soft-shading choice, and the render2d references (check_supported now raises on them). Keep the positional-directory four-split mode and every flag bigcorpus.generate_shard passes: --n-train --n-val --n-test --n-edits --seed --n-workers --compression-level --n-objects --frames --boundary --fixed-reflectivities --always-in-frustum --obs-res --drop-edge-rays --radius --max-edit-attempts --position-noise --obs-noise-std --blink-prob --blink-mean --blink-max --blink-warmup --soft-shading --soft-profile-power --n-observers --region.
- from **rayworld**, `scripts/train.py`: Replace {'corpus': str(bc.OUT)} (an absolute path written into config.json) with str(bc.OUT.relative_to(bc.REPO)); set CANONICAL_INSTANCE['rayworld'] to 'standard'.
- from **othello**, `scripts/make_othello_edits.py`: Drop the shipped_length_distribution import and the --length 0 ('Li's 5-30 mix') option. Make --length required, or default it to 20 with no 0 path, and use lengths = {a.length: a.n}. Drop layout.ensure_marker(...). Write cases_<n>.pkl and its .json to pim.environments.othello.bench.cases_path(a.instance) and its parent, instead of layout.edits_dir(..., 'v1'). Remove the 'shipped set' wording from the docstring.
- from **othello**, `scripts/bayes_floor.py`: Informational: othello/bayes.py documents the output as runs/_baselines/othello/<instance>/bayes_floor.json, per SPEC. bayes_floor() writes 'instance': <bare instance> and 'version': '1.0'.
- from **core**, `scripts/train.py`: Remove the transformer_s / recurrent_l --arch choices and their _model_config branches (build() now raises KeyError for them); remove --objective mse_onehot and the mc['output_kind'] = 'raw' line (token_source raises for objective != 'ce'; the registry ignores output_kind); drop the related docstring lines and the dated comment.
- from **export**, `scripts/make_edit_selection.py, scripts/make_rayworld_tokens.py`: Write selection.json 'vocab' repo-relative (datasets/rayworld/8-ray/tokens/vocab.npz) and drop its 'created' timestamp; tokens/meta.json is not in the bundle (the tokenizer regenerates it together with train.i16).

## to tables
- from **rayworld**, `pim/figures/tables.py`: target_cells() now returns None for out-of-scope targets (appearance-d2/-d3/-lat, grid-*-fac, full@...) and raises ValueError for obs5's appearance-fac (multi-observer). table_gridified should whitelist the in-scope targets (appearance-fac, and on 8-ray appearance, grid-6x5, grid-10x3, grid-16x8, pos@appearance), or rely on export stripping those blocks.
- from **core**, `pim/figures/tables.py`: Drop the transformer_s / recurrent_l / transformer_s_tokens entries from ARCH_LABEL and the recurrent branch near lines 561-570; those architectures no longer exist (dead code, nothing breaks).
- from **export**, `pim/figures/tables.py`: Key floors (baselines.json) and Bayes floors by (env, instance) and read them from runs/_baselines/<env>/<inst>/: the shipped files carry a bare instance ('standard' exists in both environments). For the table-diff gate against reports/reference_tables.json: ignore the 'ND ...' columns (ND removed), expect rep_sd.dropped_steps [421875] -> [] for rayworld/standard and othello/adjacent-flip, and expect no rep_sd for (rayworld/8-ray, pos@appearance).
