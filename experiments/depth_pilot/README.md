# depth_pilot

**Question:** which depth coordinate makes discworld state most linearly decodable — `y`, `rho`, `1/y`, `1/rho`, or apparent width? **Status:** done 2026-09-01; `1/y` won and became `frustum.CANONICAL_DEPTH`. Result: `scores/depth_pilot_L-dw-20m.json`; write-up in `research/findings/state-geometry.md` and the `frustum.py` docstring.
